<div class="container">
    <div class="row">
        {{#each ads}}
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="{{this.imagePath}}" alt="image" width="80px" class="img-responsive">
                    <div class="caption">
                        <h3>{{this.title}}</h3>
                        <p><strong>Category:</strong> <a href="/category/{{this.category.id}}"> {{this.category.name}}</p></a>
                        <p><strong>Town:</strong> {{this.town.name}}</p>
                        <p><strong>Price:</strong> {{this.price}}</p>
                        <div class="clearfix">
                            <div class="price pull-left">{{this.content}}</div>
                            <a href="/ad/details/{{this.id}}" class="btn btn-sm btn-default pull-right" role="button">Read more</a>
                        </div>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>