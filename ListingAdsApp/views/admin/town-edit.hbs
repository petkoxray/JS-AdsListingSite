<div class="container">
    <form class="form-horizontal" method="POST" action="/admin/town-edit/{{town.id}}">
        <fieldset>
            <legend>Edit Town</legend>

            <div class="form-group">
                <div class="col-sm-4 ">
                    <input type="text" class="form-control" id="townName" placeholder="Town Name" value="{{town.name}}"
                           name="name" required>
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-4">
                    <button type="submit" class="btn btn-success">Edit</button>
                </div>
            </div>
        </fieldset>
    </form>


    <table class="table table-responsive table-hover table-bordered">
        <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Category</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        {{#each ads}}
            <tr>
                <td><a href="/ad/details/{{id}}">{{title}}</a></td>
                <td><a href="/admin/user-edit/{{this.author.id}}">{{this.author.fullName}}</a></td>
                <td><a href="/admin/category-edit/{{category.id}}">{{category.name}}</a></td>
                <td>
                    <a href="/ad/edit/{{id}}">
                        <button type="submit" class="btn btn-primary btn-xs">Edit</button>
                    </a>
                    <a href="/ad/delete/{{id}}">
                        <button type="submit" class="btn btn-danger btn-xs">Delete</button>
                    </a>
                </td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>